<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.test">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/basket"
        android:label="Basketball"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MichaelITeamScope"></activity>
        <activity android:name=".ServerCommandMichaelI" />
        <activity android:name=".MichaelIMainActivity" />
        <activity android:name=".ThirdActivity3" />
        <activity android:name=".ServerCommand" />
        <activity android:name=".ServerCommand.kt" />
        <activity android:name=".ServercommandActivity" />
        <activity android:name=".SecondActivity2" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>